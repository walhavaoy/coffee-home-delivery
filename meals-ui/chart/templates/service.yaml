apiVersion: v1
kind: Service
metadata:
  name: meals-ui
  labels:
    app: meals-ui
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
      protocol: TCP
  selector:
    app: meals-ui
